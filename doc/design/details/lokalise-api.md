# Details regarding lokalise API

As part of the import of local messages and translations, we need to access lokalise, to do this, we make use of their ruby API.

We have an API key that is stored under AWS Secrets Manager for the dev environment under the key `lokalise/apiKey`.

