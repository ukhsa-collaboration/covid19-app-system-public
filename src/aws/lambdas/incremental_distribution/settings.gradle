plugins {
    id("de.fayard.refreshVersions").version("0.30.2")
}

refreshVersions {
    enableBuildSrcLibs()
}

rootProject.name = 'javalambda'

include 'infra'
include 'cta'
include 'pubdash'
include 'analytics'
