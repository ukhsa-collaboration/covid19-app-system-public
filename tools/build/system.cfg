##Relative paths are relative to the configuration file

#the project root directory
base=../../
#the build output directory
out=../../out

gaudi_modules=zuehlke, nhsx

import config/gitversion.config
